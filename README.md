# Portfolio (frontend)

Vite + React + TypeScript + Tailwind + shadcn/ui.

## Scripts

- `npm run dev` — local dev server
- `npm run build` — production build
- `npm run preview` — preview production build

## Environment

Set `VITE_API_BASE_URL` in `.env` for your backend API base URL.
